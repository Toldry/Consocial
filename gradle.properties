kotlin.code.style=official

# Use plain console output to prevent Gradle's progress reporter from mangling
# interactive stdin/stdout when running console applications via `./gradlew run`.
org.gradle.console=plain
